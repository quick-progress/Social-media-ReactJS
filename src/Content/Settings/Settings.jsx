import React from "react";
import './Settings.css';

const Settings = (props) => {
  return(
          <div className="settings">
            <span>Настройки</span>
          </div>
  );
};

export default Settings;